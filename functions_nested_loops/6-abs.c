#include "main.h"

/**
 * _abs - Calculates the absolute value of an integer
 * @n: The integer | process
 *
 * Return: The absolute value of n
 */

int _abs(int n)
{
	if (n < 0)
	{
		return -n;
	}

	else
	{
		return n;
	}
}
